[pytest]
markers =
    golden: golden-value regression tests (machine-pinned, run a real training job)
    slow: end-to-end training smoke tests (generate a dataset + train; minutes-scale)
testpaths = tests
